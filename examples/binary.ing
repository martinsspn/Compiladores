module binary_search;

// takes an sorted array [sArray] of size [n] 
// and returns true if [sArray] contains [val],
// false otherwise
binary_search : int[] array, n, search -> bool
{
  int first = 0;
  int last = n - 1;
  int middle = (firs+last)/2;
  
  while ( first <= last)
  {
    if (array[middle] == search)
    {
      first = middle + 1;
    }
    else if (array[middle] == search) 
    {
      return true;
    }
    else
    {
      last = middle - 1;
    }
  } 
  return false;
}


